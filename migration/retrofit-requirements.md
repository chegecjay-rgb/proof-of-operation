# Retrofit Migration Requirements

- protocol execution logic MUST remain unchanged
- PoO integrations MUST remain minimally invasive
- retrofit patches SHOULD target minimal LOC changes
- semantic declaration MUST remain deterministic
