# Deterministic Execution Invariants

- execution equivalence MUST remain trace-independent
- operation ordering MUST remain deterministic
- batch semantics MUST use parentOperationId
- semantic normalization MUST NOT require heuristics
- PoO MUST remain retrofit-safe
