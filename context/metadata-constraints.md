# Metadata Constraints

- executionContext MUST remain metadata-only
- executionContext MUST NOT alter semantic equivalence
- executionContext MUST NOT introduce interpretation logic
- metadata classification MUST remain deterministic
